{
  "private": true,
  "scripts": {
    "clean": "rm -rf output && rm -rf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "bower": "^1.8.0",
    "pulp": "10.0.4",
    "purescript-psa": "0.4.0",
    "purescript": "0.10.7"
  }
}
