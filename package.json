{
  "private": true,
  "scripts": {
    "clean": "rm -rf output && rm -rf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "bower": "^1.8.0",
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.1"
  }
}
